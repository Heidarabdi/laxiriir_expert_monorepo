{
	"name": "mobile",
	"version": "1.0.0",
	"main": "index.ts",
	"scripts": {
		"dev": "expo start --dev-client",
		"dev:go": "expo start",
		"start": "expo start",
		"android": "expo start --android",
		"ios": "expo start --ios",
		"prebuild": "expo prebuild",
		"web": "expo start --web",
		"build": "tsc --noEmit",
		"clean": "node -e \"require('node:fs').rmSync('.expo', { recursive: true, force: true }); require('node:fs').rmSync('.turbo', { recursive: true, force: true })\"",
		"lint": "biome lint .",
		"test": "node -e \"process.exit(0)\"",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@repo/env": "workspace:*",
		"@repo/platform": "workspace:*",
		"@tanstack/react-query": "^5.90.21",
		"expo": "catalog:",
		"expo-dev-client": "~55.0.16",
		"expo-status-bar": "~55.0.4",
		"react": "catalog:",
		"react-native": "catalog:",
		"zustand": "^5.0.11"
	},
	"devDependencies": {
		"@biomejs/biome": "catalog:",
		"@types/react": "catalog:",
		"typescript": "catalog:"
	},
	"private": true
}
