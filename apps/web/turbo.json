{
	"extends": ["//"],
	"tasks": {
		"build": {
			"env": ["VITE_API_BASE_URL"],
			"inputs": ["$TURBO_DEFAULT$", ".env", ".env.*"],
			"outputs": ["dist/**", ".output/**"]
		},
		"dev": {
			"env": ["VITE_API_BASE_URL"]
		}
	}
}
