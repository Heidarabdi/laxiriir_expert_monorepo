# Hoist shared dependencies (crucial for React Native / Expo)
public-hoist-pattern[]=*
shamefully-hoist=true

# Strict peer dependencies
auto-install-peers=true
strict-peer-dependencies=false

# Engine strictness
engine-strict=true
